# 1bit-MONSTER

Landing page for [1bit.monster](https://1bit.monster), served via GitHub Pages.

## Local preview

Open `index.html` directly in a browser, or serve it:

```
python3 -m http.server 8000
```

## Deploy

Pushes to `main` deploy automatically via GitHub Pages. Custom domain is set via the `CNAME` file.
