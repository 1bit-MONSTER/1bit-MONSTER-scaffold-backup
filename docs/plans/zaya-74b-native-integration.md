---
Type: plan
Context: Native ZAYA1-74B HIP integration — gguf_to_onebp → .1bp → zaya_init_onebp (issue #1521; loader strictness #1527)
Status: PARTIALLY SHIPPED — the `gguf_to_onebp` converter step landed (PR #1546, 2026-08-08), producing a verified `ZAYA1-74B.1bp` (43.6 GiB, round-trip Pearson r=0.994-0.999, zero NaNs). The `zaya_init_onebp()` wiring below is still blocked: the 74B needs ~145GB fp16 GPU expert buffers against a 62GB Strix pool, and Zaya's router "EDA" temporal-state semantics have no working reference implementation to verify against (the 8B `.1bp` loads end-to-end but its output isn't conditioned on the prompt). See [journey.md UPDATE 31](../journey.md#update-31-2026-08-08-the-tilefuse-day--npu-kernel-from-scratch-q4nx-pivot-converter-ladder), "Zaya GGUF/.1bp conversion" section.
Date: 2026-08-05
---

# Implementation Plan: Native ZAYA1-74B integration into the C++ engine

> Generated by the planni workflow (scout + planner), 2026-08-05.

## Goal
Wire the 74B Zaya GGUF → OneBP → native HIP engine so `backend_hip.cpp` loads quantized Zaya models through the existing CCA attention + MoE kernels, reusing `ZayaState`/`LayerW` structs and the `generate()` path unchanged.

## Plan

### 1. Create `zaya_init_onebp()` in `zaya_engine.cpp`
Add a new function after `zaya_init()` that:
- Opens a `.1bp` file via `OnebpModel::load()`
- Validates dimensions against `eng` config (same embed-size gate as `zaya_init`)
- Allocates all GPU buffers identically to `zaya_init()` (factor into a reusable `zaya_alloc_buffers(ZayaState*)` helper called by both init paths)
- Loads weights via the tensor name mapping below, using `OnebpModel::get_tensor_f32()` for ndim=1/2, `get_tensor_f32_expert()` for ndim=3 MoE stacks
- Uploads to GPU via `upf16`/`upf32` (already defined)
- Returns `ZayaState*`

### 2. Factor GPU buffer allocation into a shared helper
Extract the ~80 lines of GPU `hipMalloc` for buffers (`d_hs`, `d_ao`, `d_tmp`, `d_fnw`, `d_lm_out`, `d_lm_vocab`, `d_embed`, `d_ibias`, `d_iscale`, `d_token_id`, `d_conv`, `d_phs`, `d_vrec`, `d_qout`, `d_kout`, `d_vout`, `d_skip_flag`, `d_prev_rs`, `d_expert_idx`, `d_expert_wt`, `d_sorted_ids`, `d_expert_counts`, `d_expert_offsets`, `d_argmax_idx`, `d_argmax_val`, KV cache, `d_partials`) into a static function `zaya_alloc_buffers(ZayaState* s)` called by both `zaya_init()` and `zaya_init_onebp()`.

### 3. Modify `backend_hip.cpp::init()` to detect OneBP/Q4NX format
Replace the hard `.bin` gate with:
1. Try `.1bp` file in `weights_dir` (scan directory, same pattern as `backend_laguna.cpp`)
2. If found → call `zaya_init_onebp(wd.c_str(), &zcfg)` instead of `zaya_init()`
3. If no `.1bp`, try legacy `.bin` check → `zaya_init()` (existing path)
4. If neither → return false

### 4. Tensor name mapping table (GGUF `blk.N.*` → LayerW field)
Define `load_layer_onebp(OnebpModel& model, int il, LayerW& l, const ZayaConfig& eng, hipStream_t st)`:

| OneBP tensor name | LayerW field | Shape | Load fn |
|---|---|---|---|
| `blk.N.attn_norm.weight` | `nw` | [H] | get_tensor_f32 → upf16 |
| `blk.N.attn_q.weight` | `wq` | [QD, H] | get_tensor_f32 → upf16 |
| `blk.N.attn_k.weight` | `wk` | [KD, H] | get_tensor_f32 → upf16 |
| `blk.N.cca_val_proj1.weight` | `wv1` | [KD/2, H] | get_tensor_f32 → upf16 |
| `blk.N.cca_val_proj2.weight` | `wv2` | [KD/2, H] | get_tensor_f32 → upf16 |
| `blk.N.attn_output.weight` | `wo` | [H, QD] | get_tensor_f32 → upf16 |
| `blk.N.attn_norm_2.weight` | `pan` | [H] | get_tensor_f32 → upf16 |
| `blk.N.cca_conv_grp.weight` | `cdw` | [QKV, 2] | get_tensor_f32 → upf32 |
| `blk.N.cca_conv_grp.bias` | `cdb` | [QKV] | get_tensor_f32 → upf32 |
| `blk.N.cca_conv_grp_grouped.weight` | `cgw` | [QKV, 128*2] | get_tensor_f32 → upf32 |
| `blk.N.cca_conv_grp_grouped.bias` | `cgb` | [QKV] | get_tensor_f32 → upf32 |
| `blk.N.cca_k_scale.weight` | `ks` | [NKV] | get_tensor_f32 → upf32 |
| `blk.N.res_scale_hs.weight` | `pahss` | [H] | get_tensor_f32 → upf32 |
| `blk.N.res_scale_hs.bias` | `pahsb` | [H] | get_tensor_f32 → upf32 |
| `blk.N.res_scale_res.weight` | `parss` | [H] | get_tensor_f32 → upf32 |
| `blk.N.res_scale_res.bias` | `parsb` | [H] | get_tensor_f32 → upf32 |
| `blk.N.ffn_gate.weight` | `gdw` | [H, rtr_h] | get_tensor_f32 → transpose → upf32 |
| `blk.N.ffn_gate.bias` | `gdb` | [rtr_h] | get_tensor_f32 → upf32 |
| `blk.N.ffn_norm.weight` | `rfn` | [rtr_h] | get_tensor_f32 → upf32 |
| `blk.N.zaya_router_mlp2.weight` | `rf1` | [rtr_h, rtr_h] | get_tensor_f32 → upf32 |
| `blk.N.zaya_router_mlp2.bias` | `rf1b` | [rtr_h] | get_tensor_f32 → upf32 |
| `blk.N.zaya_router_mlp4.weight` | `rf2` | [rtr_h, rtr_h] | get_tensor_f32 → pad → upf32 |
| `blk.N.zaya_router_mlp4.bias` | `rf2b` | [rtr_h] | get_tensor_f32 → upf32 |
| `blk.N.mlp.gate.router_mlp.out_proj.weight` | `rout` | [n_exp_t, rtr_h] | get_tensor_f32 → upf32 |
| `blk.N.zaya_router_biases.weight` | `bb` | [n_exp_t] | get_tensor_f32 → upf32 |
| `blk.N.ffn_gate_up_exps.weight` | `gu` | [NE, 2*n_ff, H] | get_tensor_f32_expert × NE → upf16 |
| `blk.N.ffn_down_exps.weight` | `dn` | [NE, H, n_ff] | get_tensor_f32_expert × NE → upf16 |
| `blk.N.res_scale_hs.mlp.weight` | `pmhss` | [H] | get_tensor_f32 → upf32 |
| `blk.N.res_scale_hs.mlp.bias` | `pmhsb` | [H] | get_tensor_f32 → upf32 |
| `blk.N.res_scale_res.mlp.weight` | `pmrss` | [H] | get_tensor_f32 → upf32 |
| `blk.N.res_scale_res.mlp.bias` | `pmrsb` | [H] | get_tensor_f32 → upf32 |

Global tensors (loaded once):
| `token_embd.weight` | `embed` | [V, H] | get_tensor_f32 → upf16 |
| `output_norm.weight` or `model.norm.weight` | `fnw` | [H] | get_tensor_f32 → upf16 |

### 5. Handle 3D MoE expert packing
`ffn_gate_up_exps.weight` ndim=3 `[NE, 2*n_ff, H]`, tiled per-expert. `zaya_engine` expects `gu` as one contiguous fp16 buffer `[NE * 2 * n_ff * H]` with gate/up interleaved. Load each expert via `get_tensor_f32_expert()`, concatenate, then `upf16`. Same for `ffn_down_exps` `[NE, H, n_ff]` → `dn` `[NE * H * n_ff]`.

### 6. Handle 74B-specific dimension quirks
- `attn_output.weight` shape `[QD/2, H]` (half-dim CCA output). Kernel expects `wo` `[H, QD]`. Verify-or-transpose on load.
- `zaya_router_mlp4.weight`: GGUF stores `[NE+1, rtr_h]` = `[25, 256]` for 74B (24 experts + MOD skip slot; measured in `benchmarks/RESULTS-zaya1-74b-benchmark-2026-08-05.md` — the earlier `[9, 256]`/8-expert figure was wrong). Engine expects `[256, 256]`. Pad with zeros to square (pattern from `tests/zaya_gpu_decode.cpp`).
- `ffn_gate.weight`: GGUF stores `[H, rtr_h]`, zaya expects transposed `[rtr_h, H]`. Transpose on load.
- KV cache: 120 layers × 4096 seq × 2 NKV × 128 HD × 2 = ~250 MB fp16 (measured 74B dims; 8B defaults are 40 layers × 2 NKV). `ALLOC_OR_FAIL` handles OOM.

### 7. Add `zaya_init_onebp()` declaration to `zaya_engine.h`
```cpp
ZayaState* zaya_init_onebp(const char* onebp_path, const ZayaConfig* cfg = nullptr);
```

### 8. Update CMakeLists.txt
Ensure `zaya_engine.cpp` links against `onebp_loader.cpp`'s object (`OnebpModel::load()`, `get_tensor_f32()`, `get_tensor_f32_expert()` defined in `engine/npu/src/onebp_loader.cpp`).

## Files to Modify
- **`src/zaya_engine.cpp`** — Add `zaya_alloc_buffers()` helper; add `zaya_init_onebp()` (~150 lines weight-loading + ~10 factoring allocs)
- **`src/zaya_engine.h`** — Add `zaya_init_onebp()` declaration to C ABI block
- **`src/backend_hip.cpp`** — Replace `.bin`-only gate with format detection (~20 lines)
- **`CMakeLists.txt`** — Link `onebp_loader` object into the HIP engine library

## New Files
None required.

## Risks
1. **OneBP tensor name conventions**: `gguf_to_onebp.cpp` preserves GGUF names verbatim. If 74B GGUF uses HF-style `model.layers.N.*` instead of `blk.N.*`, mapping misses. Mitigation: fallback aliases (pattern from `zaya_gpu_decode.cpp`'s `add_alias()`).
2. **zaya_router_mlp4 square-pad**: 74B has 24 experts (25 slots w/ skip token — measured in the benchmark doc), so `mlp4` is `[25, 256]` not `[256, 256]`. Pad-to-square works but untested on 74B shapes. Padded invalid expert slots get zero logit (correct).
3. **MoE kernel shared-memory limits**: `zaya_router_moe.hip`, `zaya_moe_batch_union.hip` use compile-time smem. Batch path may fail for non-8B. But `generate()` uses single-token path (`zaya_forward_greedy`) via dynamic kernels (`zaya_gpu_router.hip`, `zaya_moe_tiled_gemv.hip`) — main path safe.
4. **VRAM for 74B embedding**: `vocab * H * 2` = 262K × 8192 × 2 ≈ 4.3 GB just for embedding table. `ALLOC_OR_FAIL` handles OOM.
5. **`input_hidden_states_scale/bias`**: 74B GGUF may not have these — should be optional (default scale=1.0, bias=0.0). Pattern in `zaya_gpu_decode.cpp`.
